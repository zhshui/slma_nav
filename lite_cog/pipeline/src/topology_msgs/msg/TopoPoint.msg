# A named point on the topology map
string name      # point name (e.g., "NAV_POINT_0")
float64 x        # x coordinate
float64 y        # y coordinate
float64 theta    # orientation (yaw angle)
int32 type       # point type code (0=normal, etc.)
